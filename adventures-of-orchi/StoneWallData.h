#pragma once
#include "pch.h"
#include <BasicMath.h>
#include "BaseSpriteData.h"

class StoneWallData : public BaseSpriteData
{
public:
	StoneWallData(int column, int row, float x, float y) : BaseSpriteData(column, row, x, y)
	{

	}

protected:

private:
};