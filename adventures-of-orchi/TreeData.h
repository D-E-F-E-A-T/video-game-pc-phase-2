#pragma once
#include "pch.h"
#include "BaseSpriteData.h"
#include "BasicMath.h"

class TreeData : public BaseSpriteData
{
public:
	TreeData(int column, int row, float x, float y) : BaseSpriteData(column, row, x, y)
	{
	
	}
};