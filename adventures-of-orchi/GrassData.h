#pragma once
#include "pch.h"
#include "BaseSpriteData.h"
#include <BasicMath.h>

class GrassData : public BaseSpriteData
{
public:
	GrassData(int column, int row, float x, float y) : BaseSpriteData(column, row, x, y)
	{

	}

protected:

private:
};