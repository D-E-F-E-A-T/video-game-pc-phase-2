#pragma once
#include "pch.h"
#include "BaseSpriteData.h"
#include <BasicMath.h>

class WaterData : public BaseSpriteData
{
public:
	WaterData(int column, int row, float x, float y) : BaseSpriteData(column, row, x, y)
	{

	}

protected:

private:
};