#pragma once
#include "pch.h"
#include "BaseSpriteData.h"
#include "BasicMath.h"

class HeartData : public BaseSpriteData
{
public:
	HeartData(int column, int row, float x, float y) : BaseSpriteData(column, row, x, y)
	{

	}
};